# CRM-система для управления информацией о продавцах и их транзакциях

# Содержание
- [О проекте](#о-проекте)
- [Функциональность](#функциональность)
- [Требования](#требования)
- [Быстрый старт](#🚀-быстрый-старт)
- [API Документация](#api-документация)
- [Технологический стек](#технологии)

## 📝 О проекте

**CRM System** — это упрощенная система управления взаимоотношениями с клиентами (CRM), разработанная для управления данными о продавцах и их финансовых транзакциях.

### Основные возможности:

- 📊 **Управление продавцами** — полный CRUD для работы с данными продавцов
- 💰 **Учет транзакций** — регистрация и отслеживание продаж с разными типами оплаты
- 📈 **Аналитика** — выявление самых продуктивных продавцов и анализ продаж
- 📖 **Swagger документация** — интерактивная документация API
- ✅ **Валидация данных** — проверка корректности входных данных
- 🔒 **Обработка ошибок** — централизованная обработка исключений
  
---

## ✨ Функциональность

### 1. Управление продавцами

- ✅ Получение списка всех продавцов
- ✅ Получение информации о конкретном продавце
- ✅ Создание нового продавца
- ✅ Обновление данных продавца (PATCH и PUT)
- ✅ Удаление продавца

### 2. Управление транзакциями

- ✅ Получение списка всех транзакций
- ✅ Получение информации о конкретной транзакции
- ✅ Создание новой транзакции для продавца
- ✅ Получение всех транзакций конкретного продавца
- ✅ Удаление транзакции

### 3. Аналитика

- ✅ **Самый продуктивный продавец** — поиск продавца с наибольшей суммой продаж за период (день/месяц/квартал/год)
- ✅ **Фильтрация по сумме** — список продавцов с суммой транзакций ниже указанного порога

### 4. Типы оплаты

Система поддерживает три типа оплаты:
- 💵 **CASH** — наличные
- 💳 **CARD** — банковская карта
- 🏦 **TRANSFER** — банковский перевод

## Требования
- **Java** - 17 и выше
- **Gradle** - 8.x(включен в wrapper)
- **Docker** - для запуска PostgreSQL(опционально)
- **PosgreSQL** - 15* (если без Docker)
  
## 🚀 Быстрый старт
### 1. Клонирование репозитория
```bash
git clone git@github.com:pirojohek/simple-CRM.git
```
### 2. Запуск базы данных

Проект использует **PostgreSQL** в качестве основной базы данных. Все таблицы находятся в схеме `storage`.

**Параметры подключения:**
- **Host**: localhost
- **Port**: 54321
- **Database**: storage
- **Username**: user
- **Password**: password
- **Schema**: storage

### 🐳 Запуск PostgreSQL через Docker

### Расположение файла

Docker Compose конфигурация находится по адресу:
```
src/main/resources/docker/docker-compose.yml
```

### Команды для работы с Docker
```bash
# Перейти в директорию с docker-compose.yml
cd src/main/resources/docker

# Запустить контейнер в фоновом режиме
docker-compose up -d

# Проверить статус контейнера
docker-compose ps
```

**Ожидаемый вывод:**
```
NAME                IMAGE         STATUS         PORTS
database_host       postgres:16   Up 2 minutes   0.0.0.0:54321->5432/tcp
```

#### Остановка базы данных

```bash
# Остановить контейнер (данные сохраняются)
docker-compose stop

# Остановить и удалить контейнер (данные сохраняются в volume)
docker-compose down

# Удалить контейнер и все данные
docker-compose down -v
```
### 🔌 Подключение к базе данных

### Через приложение (Spring Boot)

Конфигурация находится в `src/main/resources/application.yaml`:

```yaml
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:54321/storage
    username: user
    password: password
```

Если БД запускается не через Docker, необходимо изменить параметры application.yaml и url, name, password для БД.
Таже в application.yaml подключены data.sql и schema.sql, если есть необходимость в тестовых данных, раскомментируйте строку spring:sql:init:data-locations


### 3. Сборка проекта

```bash
# Вернуться в корневую директорию
cd ../../../..

# Собрать проект
./gradlew clean build
```

### 4. Запуск приложения

```bash
./gradlew bootRun
```

**Приложение запустится на порту 8080.**

### 5. Проверка работоспособности

Откройте в браузере:

- **Swagger UI**: http://localhost:8080/swagger-ui.html
- **API Docs JSON**: http://localhost:8080/api-docs

## 📖 API Документация

### Swagger UI (интерактивная документация)

После запуска приложения откройте в браузере:

🔗 **http://localhost:8080/swagger-ui.html**

Swagger UI предоставляет:
- 📋 Полный список всех API endpoints
- 📝 Описание параметров и моделей данных
- ▶️ Возможность выполнять запросы прямо из браузера
- 📊 Примеры запросов и ответов
- 🔍 Коды ответов и описание ошибок

### Группировка API

API разделено на 3 логические группы:

#### 1. **Sellers** — Управление продавцами
```
GET    /api/sellers              - Получить всех продавцов
GET    /api/sellers/{id}         - Получить продавца по ID
POST   /api/sellers              - Создать продавца
PATCH  /api/sellers/{id}         - Частично обновить продавца
PUT    /api/sellers/{id}         - Полностью заменить данные продавца
DELETE /api/sellers/{id}         - Удалить продавца
```

#### 2. **Transactions** — Управление транзакциями
```
GET    /api/transactions                        - Получить все транзакции
GET    /api/transactions/{id}                   - Получить транзакцию по ID
GET    /api/sellers/{sellerId}/transactions     - Получить транзакции продавца
POST   /api/sellers/{sellerId}/transactions     - Создать транзакцию для продавца
DELETE /api/transactions/{id}                   - Удалить транзакцию
```

#### 3. **Analytics** — Аналитика
```
GET /api/analytics/top-seller                           - Самый продуктивный продавец
GET /api/analytics/sellers/transactions/sum-less-than   - Продавцы с суммой ниже порога
```

### OpenAPI Specification

Спецификация OpenAPI 3.0 доступна в JSON формате:

🔗 **http://localhost:8080/api-docs**

Этот JSON можно импортировать в Postman, Insomnia или другие API клиенты.

---

## 🛠 Технологии

### Backend Framework
- **Java 21** — современная версия языка программирования
- **Spring Boot 3.5.9** — фреймворк для создания standalone приложений
- **Spring Data JPA** — упрощенная работа с базой данных через JPA
- **Spring Web** — создание RESTful API
- **Spring Validation** — валидация входных данных

### База данных
- **PostgreSQL 16** — реляционная СУБД для production
- **H2 Database** — in-memory база данных для тестирования

### Документация API
- **SpringDoc OpenAPI 3 (v2.7.0)** — автоматическая генерация Swagger/OpenAPI документации

### Библиотеки и утилиты
- **Lombok** — уменьшение boilerplate кода
- **MapStruct 1.5.5** — маппинг между Entity и DTO
- **Jakarta Validation** — аннотации для валидации

### Инструменты разработки
- **Gradle 8.14.3** — система сборки проекта
- **JUnit 5** — фреймворк для тестирования
- **Mockito** — мокирование зависимостей в тестах
- **Docker** — контейнеризация PostgreSQL
